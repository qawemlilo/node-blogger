#!/usr/bin/env node

/*
   This file contains the program that compiles all markdown files to html files
   bin
*/

function compile() {
    var Index = require('../lib/compile.index'),
        Posts = require('../lib/compile.posts');
    
    new Index();
    new Posts();
}

/*
  Start compiling
*/
compile();

